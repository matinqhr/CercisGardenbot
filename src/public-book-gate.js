import{parts,sendPage}from"./book-renderer.js";
const tg=async(e,m,b={})=>(await fetch(`https://api.telegram.org/bot${e.BOT_TOKEN}/${m}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(b)})).json();
const resp=async(e,m,b={})=>new Response(JSON.stringify(await tg(e,m,b)),{status:200,headers:{"content-type":"application/json"}});
const esc=v=>String(v??"").replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;");
async function list(e,c,mid,page=0){const rows=(await e.DB.prepare("SELECT id,url,title FROM tracks ORDER BY id ASC").all()).results||[];const size=15,total=Math.max(1,Math.ceil(rows.length/size)),p=Math.min(Math.max(Number(page)||0,0),total-1),chunk=rows.slice(p*size,(p+1)*size);let t=`📜 <b>فهرست پرونده‌های ثبت‌شده</b>\n\n📖 صفحه ${p+1} از ${total}\n📚 تعداد کل: <b>${rows.length}</b>\n\n`;for(let i=0;i<chunk.length;i++){const r=chunk[i],number=p*size+i+1,title=esc(r.title||`پرونده #${r.id}`);t+=`${number}. 🏷️ <b>${title}</b>\n🔗 <a href="${esc(r.url)}">مشاهده پست اصلی</a>\n\n`}const row=[];if(p>0)row.push({text:"←",callback_data:`public_list:${p-1}`});else row.push({text:"●",callback_data:"public_list_noop"});if(p<total-1)row.push({text:"→",callback_data:`public_list:${p+1}`});else row.push({text:"●",callback_data:"public_list_noop"});const b={chat_id:c,text:t,parse_mode:"HTML",reply_markup:{inline_keyboard:[row,[{text:"🔙 منوی اصلی",callback_data:"public_start"}]]}};if(mid){const x=await tg(e,"editMessageText",{chat_id:c,message_id:mid,...b});if(x?.ok)return new Response(JSON.stringify(x),{status:200,headers:{"content-type":"application/json"}})}return resp(e,"sendMessage",b)}
export default{async fetch(req,e){if(req.method!=="POST")return import("./admin-book-gate-v2.js").then(m=>m.default.fetch(req,e));let u;try{u=await req.clone().json()}catch{return import("./admin-book-gate-v2.js").then(m=>m.default.fetch(req,e))}const q=u?.callback_query,d=String(q?.data||"");if(q?.id)await tg(e,"answerCallbackQuery",{callback_query_id:q.id});if(d==="book_noop"||d==="public_list_noop")return new Response("ok",{status:200});if(d==="book_index"){const rows=(await e.DB.prepare("SELECT id,url,title FROM tracks ORDER BY id ASC").all()).results||[],size=15,total=Math.max(1,Math.ceil(rows.length/size));let t=`📜 <b>فهرست پرونده‌های ثبت‌شده</b>\n\n📖 صفحه 1 از ${total}\n📚 تعداد کل: <b>${rows.length}</b>\n\n`;for(let i=0;i<Math.min(size,rows.length);i++){const r=rows[i];t+=`${i+1}. 🏷️ <b>${esc(r.title||`پرونده #${r.id}`)}</b>\n🔗 <a href="${esc(r.url)}">مشاهده پست اصلی</a>\n\n`}const row=[];if(total>1)row.push({text:"→",callback_data:"public_list:1"});else row.push({text:"●",callback_data:"public_list_noop"});return resp(e,"editMessageText",{chat_id:q.message.chat.id,message_id:q.message.message_id,text:t,parse_mode:"HTML",reply_markup:{inline_keyboard:[row,[{text:"🔙 منوی اصلی",callback_data:"public_start"}]]}})}if(d.startsWith("public_list:")){const p=Math.max(0,Number(d.split(":")[1])||0);return list(e,q.message.chat.id,q.message.message_id,p)}if(d.startsWith("book:")&&!String(e.ADMIN_ID||"").split(",").map(x=>x.trim()).includes(String(q.from?.id))){const [,id,p]=d.split(":");return sendPage(e,q.message.chat.id,Number(id),Number(p)||1,q.message,"list")}return import("./admin-book-gate-v2.js").then(m=>m.default.fetch(new Request(req.url,{method:req.method,headers:req.headers,body:JSON.stringify(u)}),e))}};
