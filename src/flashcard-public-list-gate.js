const tg=async(e,m,b={})=>(await fetch(`https://api.telegram.org/bot${e.BOT_TOKEN}/${m}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(b)})).json();
const resp=x=>new Response(JSON.stringify(x),{status:200,headers:{"content-type":"application/json"}});
const esc=v=>String(v??"").replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;");
const replyTarget=q=>q?.message?.reply_to_message?.message_id||null;
const isMedia=m=>!!(m?.photo||m?.video||m?.audio||m?.voice||m?.document||m?.animation);
const keyboard=(page,total,id)=>{const row=[];if(page>1)row.push({text:"‹",callback_data:`flashpub:${page-1}:${id}`});if(page<total)row.push({text:"›",callback_data:`flashpub:${page+1}:${id}`});const k=[];if(row.length)k.push(row);k.push([{text:"🔙 لیست فلش‌کارت‌ها",callback_data:`flash_index:${id}:1`}]);return{inline_keyboard:k}};
const textFor=(rows,page,total)=>{const size=15,start=(page-1)*size;let t=`📜 <b>فهرست پست‌های ثبت‌شده</b>\n\n<b>صفحه ${page} از ${total}</b>\n\n`;for(let i=0;i<rows.length;i++){const r=rows[i];t+=`${start+i+1}. ${r.title?`🏷️ <b>${esc(r.title)}</b>`:`پرونده #${r.id}`}\n🔗 <a href="${esc(r.url||"")}">مشاهده پست اصلی</a>\n\n`}return t.trim()};
async function render(e,q,page,id){const c=q.message.chat.id,size=15,count=await e.DB.prepare("SELECT COUNT(*) n FROM tracks").first(),total=Math.max(1,Math.ceil(Number(count?.n||0)/size)),p=Math.min(Math.max(Number(page)||1,1),total),rows=(await e.DB.prepare("SELECT id,url,title FROM tracks ORDER BY id DESC LIMIT ? OFFSET ?").bind(size,(p-1)*size).all()).results||[];const body=Number(count?.n||0)?{chat_id:c,text:textFor(rows,p,total),parse_mode:"HTML",reply_markup:keyboard(p,total,id)}:{chat_id:c,text:"📜 <b>فهرست پست‌های ثبت‌شده</b>\n\nهنوز هیچ پستی ثبت نشده است.",parse_mode:"HTML",reply_markup:keyboard(1,1,id)};const mid=q.message.message_id,target=replyTarget(q);if(!isMedia(q.message)){const ed=await tg(e,"editMessageText",{...body,message_id:mid});if(ed?.ok)return resp(ed)}const del=await tg(e,"deleteMessage",{chat_id:c,message_id:mid});if(!del?.ok)return resp(del);return resp(await tg(e,"sendMessage",{...body,...(target?{reply_to_message_id:target}:{})}))}
async function findId(e,q){const rows=q.message?.reply_markup?.inline_keyboard||[];for(const row of rows)for(const b of row){const m=String(b?.callback_data||"").match(/^flash:(\d+):\d+$/);if(m)return Number(m[1])}return null}
export async function handle(e,q){const d=String(q.data||"");await tg(e,"answerCallbackQuery",{callback_query_id:q.id});if(d==="public_list:0:0"){const id=await findId(e,q);if(!id)return resp(await tg(e,"sendMessage",{chat_id:q.message.chat.id,text:"❌ شناسه پرونده فلش‌کارتی پیدا نشد.",reply_to_message_id:replyTarget(q)||undefined}));return render(e,q,1,id)}if(d.startsWith("flashpub:")){const [,p,id]=d.split(":");return render(e,q,Number(p)||1,Number(id)||0)}return null}
