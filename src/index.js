const FORM_TEMPLATE = `🏷️ عنوان پست
📂 نوع محتوا (اختیاری)
📋 اطلاعات اصلی (اختیاری)
📝 توضیحات (اختیاری)`;

const tg = async (env, method, body = {}) => { const r = await fetch(`https://api.telegram.org/bot${env.BOT_TOKEN}/${method}`, {method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)}); return r.json(); };
const esc=(v="")=>String(v).replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;");
const parseLink=(text="")=>{const m=text.trim().match(/^https?:\/\/t\.me\/(?:c\/)?([A-Za-z0-9_]+)\/([0-9]+)(?:\?.*)?$/);return m?{channel:m[1],post_id:Number(m[2])}:null;};
async function ensureSchema(env){await env.DB.prepare(`CREATE TABLE IF NOT EXISTS tracks (id INTEGER PRIMARY KEY AUTOINCREMENT,channel TEXT NOT NULL,post_id INTEGER NOT NULL,url TEXT NOT NULL UNIQUE,title TEXT,artist TEXT,release_date TEXT,lyrics TEXT,description TEXT,created_at TEXT DEFAULT CURRENT_TIMESTAMP)`).run();await env.DB.prepare(`CREATE TABLE IF NOT EXISTS sessions (user_id INTEGER PRIMARY KEY,step TEXT NOT NULL,channel TEXT,post_id INTEGER,url TEXT,updated_at TEXT DEFAULT CURRENT_TIMESTAMP)`).run();const cols=await env.DB.prepare("PRAGMA table_info(tracks)").all();const names=new Set((cols.results||[]).map(x=>x.name));if(!names.has("content_type"))await env.DB.prepare("ALTER TABLE tracks ADD COLUMN content_type TEXT").run();if(!names.has("details"))await env.DB.prepare("ALTER TABLE tracks ADD COLUMN details TEXT").run();}
async function getTrack(env,channel,postId){return (await env.DB.prepare("SELECT * FROM tracks WHERE channel=? AND post_id=?").bind(channel,postId).first())||null;}
function formatTrack(row){const p=[];if(row.title)p.push(`🏷️ <b>${esc(row.title)}</b>`);if(row.content_type)p.push(`📂 ${esc(row.content_type)}`);else if(row.artist)p.push(`🎹 <b>${esc(row.artist)}</b>`);if(row.release_date)p.push(`📅 ${esc(row.release_date)}`);if(row.details)p.push(`\n📋 <b>اطلاعات</b>\n${esc(row.details)}`);else if(row.lyrics)p.push(`\n✏️ <b>متن آهنگ</b>\n${esc(row.lyrics)}`);if(row.description)p.push(`\n📝 <b>توضیحات</b>\n${esc(row.description)}`);p.push(`\n🔗 <a href="${esc(row.url)}">مشاهده پست اصلی</a>`);return p.join("\n");}
function parseForm(text=""){const rawText=text.trim();const keys=[["🏷️","title"],["📂","content_type"],["📋","details"],["📝","description"]];const fields=Object.fromEntries(keys.map(([,k])=>[k,[]]));let current=null;for(const raw of text.replaceAll("\r\n","\n").split("\n")){const line=raw.trim();const found=keys.find(([emoji])=>line.startsWith(emoji));if(found){current=found[1];let value=line.slice(found[0].length).trim();const labels={title:"عنوان پست",content_type:"نوع محتوا",details:"اطلاعات اصلی",description:"توضیحات"};if(value.startsWith(labels[current]))value=value.slice(labels[current].length).trim();value=value.replace(/^[:：]/,"").trim().replace(/^\*\*/,"").replace(/\*\*$/," ").trim();if(value)fields[current].push(value);}else if(current&&line)fields[current].push(line);}const result=Object.fromEntries(Object.entries(fields).map(([k,v])=>[k,v.join("\n").trim()]));if(!result.title&&rawText&&!Object.values(result).some(Boolean))result.title=rawText;return result.title?{result}:{missing:["title"]};}
const isAdmin=(env,userId)=>Number(env.ADMIN_ID||0)===Number(userId);
async function setSession(env,userId,data){await env.DB.prepare(`INSERT INTO sessions(user_id,step,channel,post_id,url,updated_at) VALUES(?,?,?,?,?,CURRENT_TIMESTAMP) ON CONFLICT(user_id) DO UPDATE SET step=excluded.step,channel=excluded.channel,post_id=excluded.post_id,url=excluded.url,updated_at=CURRENT_TIMESTAMP`).bind(userId,data.step||"",data.channel||null,data.post_id||null,data.url||null).run();}
async function getSession(env,userId){return env.DB.prepare("SELECT * FROM sessions WHERE user_id=?").bind(userId).first();}async function clearSession(env,userId){await env.DB.prepare("DELETE FROM sessions WHERE user_id=?").bind(userId).run();}async function send(env,chatId,text,extra={}){return tg(env,"sendMessage",{chat_id:chatId,text,parse_mode:"HTML",...extra});}
async function adminPanel(env,chatId){return send(env,chatId,"👑 <b>پنل مدیریت 𝐇𝐨𝐦𝐞</b>",{reply_markup:{inline_keyboard:[[{text:"➕ افزودن پست",callback_data:"add"}],[{text:"✏️ ویرایش اطلاعات",callback_data:"edit"}],[{text:"🗑 حذف پست",callback_data:"delete"}],[{text:"📊 آمار",callback_data:"stats"}]]}});}
async function handleAdminText(env,message,session){const userId=message.from.id,chatId=message.chat.id;if(session.step==="ADD_LINK"){const parsed=parseLink(message.text);if(!parsed)return send(env,chatId,"❌ لینک معتبر نیست. دوباره ارسال کنید.");await setSession(env,userId,{step:"ADD_FORM",...parsed,url:message.text.trim()});return send(env,chatId,`📝 اطلاعات پست را ارسال کنید. فقط <b>عنوان پست</b> الزامی است؛ می‌توانید فقط عنوان را بفرستید یا هر مقدار اطلاعات دیگری که لازم می‌دانید اضافه کنید:\n\n<code>${esc(FORM_TEMPLATE)}</code>`);}if(session.step==="ADD_FORM"){const parsed=parseForm(message.text);if(parsed.missing)return send(env,chatId,"❌ عنوان پست را وارد نکرده‌اید. فقط عنوان لازم است؛ حتی می‌توانید عنوان را بدون قالب و به‌صورت یک پیام ساده بفرستید.");const d=parsed.result;await env.DB.prepare(`INSERT INTO tracks(channel,post_id,url,title,artist,release_date,lyrics,description,content_type,details) VALUES(?,?,?,?,?,?,?,?,?,?) ON CONFLICT(url) DO UPDATE SET title=excluded.title,content_type=excluded.content_type,details=excluded.details,description=excluded.description`).bind(session.channel,session.post_id,session.url,d.title,null,null,null,d.description||null,d.content_type||null,d.details||null).run();await clearSession(env,userId);return send(env,chatId,"✅ پروندهٔ پست در کتابخانه ثبت شد.");}if(session.step==="EDIT_LINK"){const parsed=parseLink(message.text);if(!parsed)return send(env,chatId,"❌ لینک معتبر نیست. دوباره ارسال کنید.");const row=await getTrack(env,parsed.channel,parsed.post_id);if(!row)return send(env,chatId,"❌ پرونده‌ای برای این پست پیدا نشد.");await setSession(env,userId,{step:"EDIT_FORM",channel:parsed.channel,post_id:parsed.post_id,url:row.url});return send(env,chatId,`📌 <b>پروندهٔ فعلی:</b>\n\n${formatTrack(row)}\n\n✏️ نسخهٔ جدید را بفرستید. فقط عنوان الزامی است؛ بخش‌های دیگر اختیاری‌اند:\n\n<code>${esc(FORM_TEMPLATE)}</code>`);}if(session.step==="EDIT_FORM"){const parsed=parseForm(message.text);if(parsed.missing)return send(env,chatId,"❌ عنوان پست الزامی است.");const d=parsed.result;await env.DB.prepare("UPDATE tracks SET title=?,content_type=?,details=?,description=? WHERE channel=? AND post_id=?").bind(d.title,d.content_type||null,d.details||null,d.description||null,session.channel,session.post_id).run();await clearSession(env,userId);return send(env,chatId,"✅ پروندهٔ پست به‌روزرسانی شد.");}if(session.step==="DELETE_LINK"){const parsed=parseLink(message.text);if(!parsed)return send(env,chatId,"❌ لینک معتبر نیست. دوباره ارسال کنید.");const row=await getTrack(env,parsed.channel,parsed.post_id);if(!row)return send(env,chatId,"❌ پرونده‌ای برای این پست پیدا نشد.");await env.DB.prepare("DELETE FROM tracks WHERE channel=? AND post_id=?").bind(parsed.channel,parsed.post_id).run();await clearSession(env,userId);return send(env,chatId,"🗑 پروندهٔ پست از کتابخانه حذف شد.");}}
async function handleCallback(env,q){const chatId=q.message.chat.id,userId=q.from.id;await tg(env,"answerCallbackQuery",{callback_query_id:q.id});if(q.data==="help")return send(env,chatId,"📚 <b>راهنمای کتابخانه</b>\n\nلینک یا خودِ پستی را که از کانال 𝐇𝐨𝐦𝐞 دریافت کرده‌اید برای من ارسال کنید.\n\nمن فقط اطلاعات پست‌هایی را ارائه می‌کنم که در آرشیو 𝐇𝐨𝐦𝐞 ثبت شده باشند.\n\nلطفاً از ارسال محتوای خارج از 𝐇𝐨𝐦𝐞 خودداری کنید.");if(q.data==="about")return send(env,chatId,"🌳 <b>Cercis Garden</b>\n\nکتابخانه‌ای از اطلاعات پست‌های ثبت‌شده در 𝐇𝐨𝐦𝐞.\n\nموسیقی تنها یکی از محتواهایی است که می‌تواند در این کتابخانه پرونده داشته باشد.");if(!isAdmin(env,userId))return;if(q.data==="add"){await setSession(env,userId,{step:"ADD_LINK"});return send(env,chatId,"🔗 لینک پست را ارسال کنید.");}if(q.data==="edit"){await setSession(env,userId,{step:"EDIT_LINK"});return send(env,chatId,"🔗 لینک پستی را که می‌خواهید ویرایش کنید ارسال کنید.");}if(q.data==="delete"){await setSession(env,userId,{step:"DELETE_LINK"});return send(env,chatId,"🔗 لینک پستی را که می‌خواهید حذف کنید ارسال کنید.");}if(q.data==="stats"){const row=await env.DB.prepare("SELECT COUNT(*) AS count FROM tracks").first();return send(env,chatId,`📊 تعداد پرونده‌های ثبت‌شده: <b>${row?.count||0}</b>`);}}
async function handleMessage(env,message){if(!message)return;const userId=message.from?.id,chatId=message.chat.id;const session=userId?await getSession(env,userId):null;if(session&&isAdmin(env,userId)&&message.text)return handleAdminText(env,message,session);if(message.text==="/start")return send(env,chatId,"🌳 <b>Cercis Garden</b>\n\nکتابخانه‌ای از اطلاعات پست‌های 𝐇𝐨𝐦𝐞.\n\nلینک یا خودِ پستی را که از 𝐇𝐨𝐦𝐞 دریافت کرده‌اید برای من ارسال کنید.",{reply_markup:{inline_keyboard:[[{text:"📚 راهنما",callback_data:"help"}],[{text:"ℹ️ درباره ربات",callback_data:"about"}]]}});if(message.text==="/admin")return isAdmin(env,userId)?adminPanel(env,chatId):send(env,chatId,"⛔ دسترسی ندارید.");const origin=message.forward_origin;if(origin?.type==="channel"){const channel=origin.chat?.username,postId=origin.message_id;const row=channel?await getTrack(env,channel,postId):null;return row?send(env,chatId,formatTrack(row)):send(env,chatId,"📚 این پست هنوز در کتابخانهٔ 𝐇𝐨𝐦𝐞 ثبت نشده است.\n\nلطفاً فقط پست‌های 𝐇𝐨𝐦𝐞 را برای من ارسال کنید.");}if(message.text){const parsed=parseLink(message.text);if(!parsed)return send(env,chatId,"❌ لطفاً لینک یک پست تلگرام را ارسال کنید.\n\nاین کتابخانه برای پست‌های 𝐇𝐨𝐦𝐞 است.");const row=await getTrack(env,parsed.channel,parsed.post_id);return row?send(env,chatId,formatTrack(row)):send(env,chatId,"📚 این پست در کتابخانهٔ 𝐇𝐨𝐦𝐞 ثبت نشده است.\n\nلطفاً فقط پست‌های 𝐇𝐨𝐦𝐞 را برای من ارسال کنید.");}}
async function setWebhook(env,url){const secret=env.WEBHOOK_SECRET?{secret_token:env.WEBHOOK_SECRET}:{};return tg(env,"setWebhook",{url,allowed_updates:["message","callback_query"],drop_pending_updates:false,...secret});}
export default{async fetch(request,env){try{if(!env.BOT_TOKEN||!env.ADMIN_ID||!env.DB)return new Response("Missing BOT_TOKEN, ADMIN_ID or DB binding",{status:500});await ensureSchema(env);const url=new URL(request.url);if(request.method==="GET"){if(url.pathname==="/"){const result=await setWebhook(env,url.origin+"/telegram");return new Response(result.ok?"Cercis Garden Bot is running.":JSON.stringify(result),{status:result.ok?200:500});}return new Response("Not found",{status:404});}if(request.method!=="POST"||url.pathname!=="/telegram")return new Response("Not found",{status:404});if(env.WEBHOOK_SECRET&&request.headers.get("X-Telegram-Bot-Api-Secret-Token")!==env.WEBHOOK_SECRET)return new Response("Unauthorized",{status:401});const update=await request.json();if(update.callback_query)await handleCallback(env,update.callback_query);else if(update.message)await handleMessage(env,update.message);return new Response("OK");}catch(e){console.error(e);return new Response("Internal error",{status:500});}}};