const tg=async(e,m,b={})=>(await fetch(`https://api.telegram.org/bot${e.BOT_TOKEN}/${m}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(b)})).json();
const out=x=>new Response(JSON.stringify(x),{status:200,headers:{"content-type":"application/json"}});
const esc=v=>String(v??"").replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;");
const buttons=(page,total)=>{const row=[];if(page>1)row.push({text:"‹",callback_data:`public_list:${page-1}`});if(page<total)row.push({text:"›",callback_data:`public_list:${page+1}`});row.push({text:"🔙 منوی اصلی",callback_data:"public_start"});return{inline_keyboard:[row]};};
const render=async(e,chatId,messageId,page)=>{const rows=(await e.DB.prepare("SELECT id,url,title FROM tracks ORDER BY id DESC").all()).results||[];const size=15;const total=Math.max(1,Math.ceil(rows.length/size));const p=Math.min(Math.max(Number.isInteger(page)?page:1,1),total);const items=rows.slice((p-1)*size,p*size);let text="📜 <b>فهرست پرونده‌های ثبت‌شده</b>\n\n";if(!items.length)text+="هنوز هیچ پرونده‌ای ثبت نشده است.";else for(let i=0;i<items.length;i++){const r=items[i];const number=rows.length-((p-1)*size+i);text+=`${number}. ${r.title?`🏷️ <b>${esc(r.title)}</b>`:`پرونده #${r.id}`}\n🔗 <a href="${esc(r.url)}">مشاهده پست اصلی</a>\n\n`}text+=`<i>صفحه ${p} از ${total}</i>`;const reply_markup=buttons(p,total);if(messageId){const edited=await tg(e,"editMessageText",{chat_id:chatId,message_id:messageId,text,parse_mode:"HTML",reply_markup});if(edited?.ok)return edited}return tg(e,"sendMessage",{chat_id:chatId,text,parse_mode:"HTML",reply_markup})};
const pageFromCallback=d=>{if(d==="public_list")return 1;const m=d.match(/^public_list:(\d+)$/);return m?Number(m[1]):null};
export default{async fetch(req,e){let u;try{u=await req.clone().json()}catch{return out({ok:true})}const q=u?.callback_query;if(!q)return out({ok:true});const d=String(q.data||"");const page=pageFromCallback(d);if(page!==null){await tg(e,"answerCallbackQuery",{callback_query_id:q.id});return out(await render(e,q.message?.chat?.id,q.message?.message_id,page))}return out({ok:true})}};